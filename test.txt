messages:
1(
person="Charlie Weiler",
message="Hello world!",

#determines weather to skip message and go to default exit
display_cond=($is_working == 1),
default_exit={r,1},

logic={
	set($first_message, 1)
	if: ($first_message == 1)
		(exit(r,3))
	else: 
		(exit(r,1))
}	
)

reply_set:
1(
replies={1,2,3},
)
#comment

replies:
1(
message="Hi there",
exit={type=m, id=1},
)

2(
message="bye!",
main_logic={
	exit(l)
}
)

3(
message="Special move",

#logic is executed every time a message/reply is visited,
#it can set variables, call functions, and determine exit location
show_logic=($first_message != 1),
main_logic={
	if (rand (1,20) > $player_stat_health):
		(set($special_val, 1), exit(m, 2))
	else: 
		(exit (m, 1))
}
exit={type=o, id="start game"},
)


